{
  "identifier": "default",
  "description": "Default capabilities for the app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:allow-save",
    "dialog:allow-open",
    "fs:default",
    "process:default"
  ]
}
